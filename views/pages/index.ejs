<!DOCTYPE html>
<html>
<head>
  <title>Netflix Series</title>
  <link rel="stylesheet" type="text/css" href="/stylesheets/main.css" />
</head>

<body>
  <div class="main-container">
    <div class="title-container">
      <h1>NETFLIX SERIALE</h1>
    </div>
    <div class="container">
      <h2>premiery listopadowe</h2>
      <table>
          <tr>
              <th>Tytuł</th>
              <th>Sezon</th>
              <th>Data premiery</th>
          </tr>
          <% results.forEach(function(r) { %>
              <tr>
                  <td><%= r.title %></td>
                  <td><%= r.season %></td>
                  <td><%= r.date %></td>
              </tr>
          <% }); %>
      </table>
    </div>
  </div>
</body>
</html>
